This is a project whose dependencies are handled by maven.

This application can be deployed locally using the following command:
    mvn jetty:run

Likewise, you can create a WAR file by running 'mvn package'

Before testing the app locally, you will need to modify the 
'daggoo4sadi.properties' file. This file contains some hard-coded paths that
are used by this application when it constructs URLs and stores temporary
files.

There is an EXAMPLE file that outlines how you can test this application.

There is also a TODO file that outlines what is left to todo, etc. If something
is missing, then please add it. Likewise, remove items that are completed.

